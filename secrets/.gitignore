# Encrypted secrets are SAFE to commit
# sops encrypts secrets with age/gpg, making them secure in version control
# Only users with the private key can decrypt them

# Allow encrypted .yaml files
!*.yaml

# Keep directory structure
!.gitkeep

# Keep this gitignore file
!.gitignore

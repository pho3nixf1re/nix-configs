# ZSH Options Configuration

# Enable matching dotfiles with glob patterns
setopt dotglob

# Allow comments in interactive mode, useful for tmux automation
setopt interactivecomments

# Disable autocorrect (it's more annoying than helpful)
unsetopt correct_all
